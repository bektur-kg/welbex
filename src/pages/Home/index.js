import * as API from './API'
import * as Hooks from './hooks'
import * as Pages from './pages'

// Object to collect necessary things for using them in internal components
export const Home = {
  API,
  Hooks,
  Pages,
}